// Domovník design system — Phase 3
// Shared design tokens and utilities for all platforms.

export * from './tokens.js';

/**
 * Returns the current design token set.
 */
export function greet(): string {
  return 'Domovník — ui package (design tokens ready)';
}