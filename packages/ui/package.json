{
  "name": "@domovnik/ui",
  "version": "0.0.1",
  "private": true,
  "description": "Domovník design tokens and shared UI components",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/",
    "test": "vitest run",
    "build": "echo 'ui — nothing to build (source-only package)'"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}